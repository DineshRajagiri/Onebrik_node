// Swagger decorators removed. This file is kept as a placeholder.
// Use class-validator and NestJS built-in tools for request validation.
export function AuthResponse(_endpoint: string) {
  return (_target: any, _key?: string, descriptor?: PropertyDescriptor) =>
    descriptor;
}
